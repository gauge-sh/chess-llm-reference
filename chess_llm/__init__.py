"""Chess vs. LLM — a minimal chess engine, an LLM opponent driven by tool calls, and
self-contained observability (games, moves, and agent traces in the database plus
structured JSON logs)."""

__version__ = "0.1.0"
