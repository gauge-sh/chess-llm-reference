# Core
chess>=1.11
SQLAlchemy>=2.0
python-dotenv>=1.0
click>=8.1

# HTTP client (calls the LLM endpoint; also used by the API test client)
httpx>=0.27

# Web API
fastapi>=0.110
uvicorn[standard]>=0.29

# Database driver. SQLite ships with Python. Uncomment to use a hosted Postgres
# by setting a postgresql+psycopg:// DATABASE_URL.
psycopg[binary]>=3.1
